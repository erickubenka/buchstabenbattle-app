import { ConnectionGameData } from './connection-game-data';

describe('ConnectionGameData', () => {
  it('should create an instance', () => {
    expect(new ConnectionGameData()).toBeTruthy();
  });
});
