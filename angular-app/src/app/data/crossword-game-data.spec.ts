import { CrosswordGameData } from './crossword-game-data';

describe('CrosswordGameData', () => {
  it('should create an instance', () => {
    expect(new CrosswordGameData()).toBeTruthy();
  });
});
